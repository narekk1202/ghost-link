{
  "name": "server",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "bun --watch run src/index.ts && tsc --watch"
  },
  "dependencies": {
    "@upstash/redis": "^1.35.7",
    "hono": "^4.10.8",
    "shared": "workspace:*"
  },
  "devDependencies": {
    "@types/bun": "^1.3.4"
  }
}
